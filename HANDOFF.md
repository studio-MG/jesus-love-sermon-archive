# Jesus' Love Church 말씀 아카이브 인수인계서

## 절대 기준

공개 GitHub Pages판은 기존 비공개 사이트의 **열람 화면을 그대로 복제한 공개 전용판**이다. 새 디자인을 만들거나 UI를 임의로 단순화하지 않는다.

기준 UI는 비공개 원본 프로젝트의 `app/ui/ArchiveClient.tsx`와 `app/globals.css`이다. 변경 전 반드시 두 파일과 실제 비공개 사이트를 비교한다.

## 반드시 유지할 항목

- 로고와 `Jesus' Love Church — THE WAY 237` 헤더
- 밝은 서재 분위기의 건축적 배경, 은은한 빛과 모션
- 5개 카테고리 탭과 모바일 2열 배치
- 월별 페이지: `이전 페이지`은 과거 월, `다음 페이지`는 최근 월
- 제목·성경 본문·내용·다양한 날짜 형식 검색 및 형광 표시
- 기존 카드 비율, 색, 모서리, 날짜 크기, 본문 미리보기
- 전체 녹취록 모달과 고정 효과 도구함
- 형광펜·굵게·밑줄·선택 지우기·전체 지우기
- 효과는 기기 로컬에만 저장하고 PDF·인쇄에는 포함하지 않음
- 모바일·태블릿·PC 반응형 구성
- 흰 A4, 검은 글자 중심의 인쇄 화면과 제목/본문 간격
- 인쇄판에서도 원본 문단·줄바꿈을 유지하고 본문 11pt 내외, 충분한 행간과 문단 간격을 보장

## 공개판에서 반드시 제외할 항목

- 관리자 페이지 및 관리자 링크
- 비밀번호, 세션, 로그인 처리
- 서버 API와 데이터베이스 연결 정보
- 토큰, 키, 로컬 사용자 경로

## 데이터 반영 원칙

공개 말씀은 `data/sermons.json`에만 넣는다. 등록·수정 전 원본 사이트의 제목, 카테고리, 날짜, 성경 본문, 녹취록을 대조한다. 공개판 데이터 변경은 현재 자동 연동되지 않으므로 별도 배포가 필요하다.

배포 전 원본과 공개판의 **카테고리별 글 개수, 전체 제목 목록, 날짜 목록**을 비교한다. 한 편이라도 차이가 있으면 배포하지 않는다. UI 이식이나 기능 수정 시에도 기존 `data/sermons.json`을 새 샘플 데이터로 덮어쓰거나 일부 카테고리만 복사하지 않는다.

## 배포 전 필수 점검

1. 기존 UI와 공개판을 데스크톱·태블릿·모바일에서 비교한다.
2. 5개 탭, 월 이동 방향, 검색, 날짜 검색, 검색어 강조를 확인한다.
3. 전체 보기, 스크롤 중 도구함 고정, 모든 읽기 효과를 확인한다.
4. 인쇄 미리보기에서 효과가 제거되고 A4 흰 배경인지 확인한다.
5. 저장소 전체에서 비밀번호, 토큰, API 키, 관리자 코드, `/Users/` 경로가 없는지 검사한다.
6. 원본과 공개판의 카테고리별 글 개수·제목·날짜가 모두 일치하는지 확인한다.
7. 기존 UI와 다른 새 디자인은 사용자 명시 승인 없이는 배포하지 않는다.

## 사고 방지 원칙

“GitHub Pages용으로 바꾼다”는 것은 호스팅 방식만 바꾸는 것이며 디자인을 바꾸라는 뜻이 아니다. 기존 기능을 구현하기 어렵다는 이유로 임의 축소하지 않는다. 불가피한 차이가 있으면 배포 전에 사용자에게 구체적으로 보고하고 승인을 받는다.
